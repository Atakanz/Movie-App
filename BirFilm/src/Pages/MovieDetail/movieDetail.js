import React from 'react';
import {SafeAreaView, Text} from 'react-native';
import styles from './movieDetail.style';

const MovieDetail = () => {
  return (
    <SafeAreaView>
      <Text>Hello MovieDetail!</Text>
    </SafeAreaView>
  );
};

export default MovieDetail;
